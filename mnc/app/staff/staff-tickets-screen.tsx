import { View } from "react-native";
import { Typography } from "@/components/common/typography";

export default function StaffTicketsScreen() {
  return (
    <View>
      <Typography variant="h4">Staff Tickets</Typography>
    </View>
  );
}