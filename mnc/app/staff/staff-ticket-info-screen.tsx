import { View } from "react-native";
import { Typography } from "@/components/common/typography";

export default function StaffTicketInfoScreen() {
  return (
    <View>
      <Typography>Staff Ticket Info</Typography>
    </View>
  );
}