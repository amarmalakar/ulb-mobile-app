/** Max size per photo before server compress (multipart upload). */
export const SERVICE_PHOTO_RAW_MAX_BYTES = 10 * 1024 * 1024;

export const SERVICE_PHOTO_MAX_COUNT = 3;

export const SERVICE_PHOTOS_R2_PREFIX = "complaint-photos";
